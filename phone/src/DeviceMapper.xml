<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="phone.server.mybatis.DeviceMapper">

	<resultMap id="DeviceResultMap"
		type="phone.shared.model.Device">
		<id property="id" column="id" />
		<result property="roomId" column="room_id" />
		<result property="deviceNumber" column="device_number" />
		<result property="operatorName" column="operator_name" />
	</resultMap>

	<select id="findAll" resultMap="DeviceResultMap">
		SELECT id, room_id,
		device_number, operator_name
		FROM device
	</select>

	<select id="findByDeviceNumber" resultMap="DeviceResultMap">
		SELECT id, room_id,
		device_number, operator_name
		FROM device WHERE device_number =
		#{device_number}
	</select>
	
	<select id="findAllByRoomId" resultMap="DeviceResultMap">
		SELECT id, room_id,
		device_number, operator_name
		FROM device WHERE room_id =
		#{room_id}
	</select>

</mapper>